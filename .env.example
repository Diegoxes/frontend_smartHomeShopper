# Origen público del backend (sin path /api; las rutas lo añaden en código).
# Render / build: debe existir en tiempo de `npm run build`.
#
# Ejemplo:
VITE_API_BASE_URL=https://backendsmarthomeshopper-production.up.railway.app
