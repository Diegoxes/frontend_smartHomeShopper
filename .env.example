# URL del API Spring (incluye /api porque es el context-path del backend)
# Railway: Variable de entorno en el servicio del FRONTEND, disponible en el paso de build.
VITE_API_BASE_URL=https://backendsmarthomeshopper-production.up.railway.app/api
