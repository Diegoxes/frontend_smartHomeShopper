# Origen del backend (sin path /api; las rutas lo añaden en código).
# Copia este archivo a .env y ajusta según entorno.
#
# Local:
VITE_API_BASE_URL=http://localhost:8080
#
# Producción (npm run build):
# VITE_API_BASE_URL=https://backendsmarthomeshopper-production.up.railway.app
